[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "myelectricaldata_import"
version = "1"
dependencies = [
    "paho-mqtt",
    "python-dateutil",
    "requests",
    "markdown",
    "influxdb-client",
    "PyYAML",
    "mergedeep",
    "Flask",
    "Flask-APScheduler",
    "jinja2",
    "art",
    "SQLAlchemy",
#    "Flask-Migrate"
]

[project.optional-dependencies]
dev = ["pytest"]
